# v0.13 功能说明

本版本只交付 Windows 客户端。界面中的三个节点可在同一个进程内模拟；硬件模式提供发现与握手入口，但尚未形成三台电脑独立运行的无线通信系统。

## 功能与验证状态

| 功能 | v0.13 状态 | 验证或边界 |
| --- | --- | --- |
| 原生 WinForms 窗口 | 已实现 | 自动渲染检查；真实桌面从 Explorer 启动 |
| X 隐藏、托盘打开/退出 | 已实现 | 自动窗体生命周期回归；退出撤销托盘图标 |
| 单实例恢复 | 已实现注册消息路径 | 同一交互桌面内再次启动请求恢复；跨沙箱桌面不作为支持场景 |
| 发现附近节点 | 模拟可用，真实协议已实现 | 模拟数据源；真实侧需配套 S3 回应 SEARCH |
| 创建、申请加入、邀请房间 | 单机模拟已实现 | 88 项核心回归覆盖关键规则 |
| leader 准入、拒绝和踢出 | 单机模拟已实现 | 非 leader 不能执行管理操作 |
| leader 正常退出继任 | 单机模拟已实现 | 按不可变 join_order 顺序继任，term 增加 |
| 成员快照与日志副本 | 单机模拟已实现 | 副本为同一进程中的对象；无跨电脑传输或磁盘持久化 |
| USB CDC 识别 | 客户端代码已实现 | 候选筛选、版本/nonce/字段校验；真机待联调 |
| 轮询暂停与恢复 | 已实现 | 单飞、每 3 秒兜底、连续失败 5 次停止定时器；手动恢复清零 |
| 插件组合与独立更新 | 已实现 | profile、服务依赖、版本校验、哈希、反向清理；重启生效 |
| 文本、小文件、游戏状态传输 | 未实现 | 计划作为传输及应用服务插件 |
| P2P 公网连接、远程唤醒、虚拟网卡 | 未实现 | 不属于本版本能力声明 |

## 房间操作规则

1. 创建者是初始 leader；新建的一人房间为等待状态，仍允许加入。
2. 申请加入不会立即成为成员，必须由当前 leader 同意。
3. 任意成员均可邀请；接受邀请后仍进入审批流程。
4. leader 可以拒绝申请、踢出成员；普通成员没有这些权限。
5. leader 正常退出后，由剩余成员中最早加入者继任。
6. 房间曾达到两人后进入正式状态；正式房间剩一人时解散。
7. 突然掉电、丢包和网络分区下的安全继任尚未实现；退出整个客户端也不会保存房间到磁盘。

## USB 与使用方式

不需要硬件演示时，以 `--demo` 启动，切换电脑 A/B/C 完成房间流程。普通启动同样加载模拟插件，但 B/C 初始离线；当前“真实硬件”入口不表示已完成独立三机同步。

串口仅探测 TriLink 描述、Espressif VID `303A` 或 `TRILINK_PORTS` 指定的候选端口；候选身份只是筛选条件，必须通过 nonce 握手才能认定节点。真实搜索收到的 Room 元数据目前会显示日志，并未自动建立远端房间副本。

暂停针对后台自动扫描；用户点击“搜索设备”是独立的显式操作。进程退出会释放插件资源；Windows 关机和注销不会被 X 隐藏逻辑拦截。当前没有安装器、系统服务或默认开机自启动。

## 运行与交付

- 目标环境：Windows、.NET Framework 4.8；离线构建使用本机 Visual Studio Roslyn 与参考程序集。
- 本机运行目录：`artifacts/Release/`，需整体保留 EXE、依赖、profile 和插件 DLL/清单。
- 测试、截图和日志：`artifacts/tests/Release/`；它们不属于源码仓库中的预编译交付物。
- 双击托盘或“打开 TriLink”恢复窗口；托盘右键“退出”结束程序。
- 没有已部署的互联网中继、VPN、商业服务器或云端数据存储依赖。

源码中具体入口见 [架构说明](ARCHITECTURE.md)，协议字段见 [USB 与 Room 契约](USB_AND_ROOM_CONTRACT.md)，后续验收见 [开发计划](ROADMAP.md)。
